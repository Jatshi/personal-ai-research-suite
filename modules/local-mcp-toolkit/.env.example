# local-mcp-toolkit does not need an API key by itself.
# For real LLM-backed RAG, point config.yaml rag.project_path to a project
# such as ../personal-ai-workspace and configure that project's .env.
