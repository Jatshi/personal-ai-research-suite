export type Chunk = { chunk_id?: string; file_name?: string; snippet?: string; text?: string; score?: number; page_number?: number; section_title?: string };
export type RagResponse = { answer?: string; confidence?: number; citations?: string[]; evidence?: Chunk[]; results?: Chunk[]; retrieval_trace?: Record<string, unknown>; final_report?: string; approval_token?: string; report?: ThesisReport; workflow?: PaperWorkflowItem[]; steps?: Array<{ tool_name?: string; agent?: string; task?: string; result?: unknown; output?: string }> };
export type ThesisReport = { structure?: { issues?: unknown[]; missing_sections?: string[] }; figures_tables_equations?: { issues?: unknown[] }; bibliography?: { issues?: unknown[] }; todos?: unknown[] };
export type PaperWorkflowItem = { file_name: string; status: string; title: string; year?: string; completed_roles: string[]; error_count: number };
export type Dashboard = { documents: number; chunks: number; collections: Array<{ name: string; count: number }>; file_types: Array<{ name: string; count: number }>; graph: { nodes: number; edges: number }; queries: { total: number; average_confidence: number | null; trend: Array<{ date: string; count: number }>; recent: Array<{ query: string; timestamp?: string; confidence?: number; collection?: string; retrieved_chunks: number }> }; recent_documents: Array<{ doc_id: string; file_name: string; collection: string; file_type: string; imported_at: string; tags?: string[] }> };
export type LogEvent = { category: string; timestamp?: string; query?: string; goal?: string; success?: boolean; trace_id?: string; [key: string]: unknown };
export const errorMessage = (error: unknown) => error instanceof Error ? error.message : "The request could not be completed.";
